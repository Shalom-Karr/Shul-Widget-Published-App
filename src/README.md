# Source code

This repository holds the **public releases, landing page, and changelog** for Shul Widget.

The full app **source code** lives in a separate repository:

### → https://github.com/Shalom-Karr/Shul-Widget

That repo is private; access is limited. It always points at the latest source —
nothing here is pinned to a specific commit.
